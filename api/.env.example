# PostgreSQL via Neon — include pgbouncer params for serverless
DATABASE_URL=postgresql://USER:PASSWORD@HOST/sovlo?pgbouncer=true&connection_limit=1

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Server
PORT=4000
NODE_ENV=development
